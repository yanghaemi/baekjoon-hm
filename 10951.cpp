#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
#define endl "\n"

int main()
{
    int a, b;
    while (!(cin >> a >> b).eof())
    {

        cout << a + b << '\n';
    }
}