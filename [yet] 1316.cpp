#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main()
{
    int n, flag, num = 0;
    cin >> n;
    vector<string> str(n);
    vector<char> ab(n);
    for (int i = 0; i < n; i++)
    {
        cin >> str[i];
        for (int j = 0; j < str[i].length(); j++)
        {
            if (ab[j] == 0 &&)
        }
    }
}